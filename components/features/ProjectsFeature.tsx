import { ProjectList } from "@/components/screens/library/ProjectList";

/** FEATCMP-PROJECTS — PROJECT-LIST + ROW */
export function ProjectsFeature() {
  return (
    <div data-featcmp="FEATCMP-PROJECTS">
      <ProjectList />
    </div>
  );
}
