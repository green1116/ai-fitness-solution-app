import { AccessControls } from "@/components/screens/entry/AccessControls";

/** FEATCMP-ACCESS — CMP-ACCESS-SIGNIN + CMP-ACCESS-LANGUAGE */
export function AccessFeature() {
  return (
    <div data-featcmp="FEATCMP-ACCESS">
      <AccessControls />
    </div>
  );
}
