# 22 页方案 PDF 基线（不可回退）

- **基线文件**：`plan_22pages.pdf`  
  请将当前的 22 页方案 PDF（如 `plan_attaguy-plan.pdf`）复制到此目录并命名为 `plan_22pages.pdf`。

- **用途**：任何后续修改若导致  
  - 页数 ≠ 22，或  
  - 章节顺序改变，  
  即视为回退，需修复后再合入。

- **对应 Git 基线**：分支 `plan22_stable`，标签 `PLAN22_STABLE`。
