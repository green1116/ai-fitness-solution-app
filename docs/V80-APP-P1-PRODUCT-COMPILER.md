# V80 APP P1 — System-to-Product Compiler

Maps sealed V80 kernel (P1–P5) → AI Fitness Solution product layer. **Spec only** — no kernel/runtime/DB changes.

## Verify

```bash
npx tsx scripts/verify-v80-app-p1-product-compiler.ts
```

## Kernel input (read-only)

`v80-system-freeze-1` · `closeV80System()` sealed
