/** Minimal stub — code scaffold entry for integrity checks. */
export function buildCodeScaffold(_input?: { deploymentId?: string }) {
  return {
    ok: true,
    version: "v80-stub-scaffold",
    modules: [] as string[],
    scaffoldReady: true,
  };
}
