/** Minimal stub — code release entry for scaffold handlers. */
export function buildCodeRelease(_input?: Record<string, unknown>) {
  return {
    ok: true,
    version: "v80-stub-release",
    artifacts: [] as string[],
    releaseReady: true,
  };
}
